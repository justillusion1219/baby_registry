# Rae 的育兒清單 — 設定說明

資料夾裡的檔案：

| 檔案 | 用途 |
|---|---|
| `index.html` | **前台**，分享給親友的那一頁 |
| `admin.html` | **後台**，直接在網頁上編輯清單，右邊有即時預覽 |
| `registry-core.js` | 共用設定與資料邏輯。**只有 `API_URL` 需要你動一次** |
| `apps-script.gs` | 後端程式，貼到 Google Apps Script |
| `設定說明.md` | 就是這份 |

> 四個檔案要放在**同一個資料夾**，不能只拿 `index.html` 出來用。

---

## A. 先看看長什麼樣（0 分鐘）

打開 `admin.html` 就能編輯，打開 `index.html` 看親友視角。

這時是**本機預覽模式**：所有改動只存在你這台電腦，其他人看不到。可以放心亂改亂試，設定完成前不會影響任何人。

---

## B. 後台怎麼用

`admin.html` 有四個分頁：

**清單項目**
每一項都直接在欄位上改，點一下別的地方就自動存檔（右上角會顯示「已儲存」）。

- **數量**：選「多多益善」的話，很多人都可以認領同一項，適合尿布、濕紙巾這種消耗品；選「1 個就好」的話，一被認領就會移到前台的「已經有人準備」。
- **參考連結**：留白也沒關係，前台會自動附上 Google 購物的搜尋連結；貼上商品網址的話就會變成「參考連結」。
- **↑ ↓**：調整順序。**✕**：刪除（可以在「設定」分頁還原）。
- 分類名稱可以直接點著改，改了底下的項目會一起跟著搬過去。

**頁面文字**
標題、開場白、預產期、收件方式、使用說明的每一步，都在這裡改。留白的欄位在前台就不會顯示。

**認領紀錄**
誰認領了什麼、留了什麼話、什麼時候登記的。可以匯出 CSV 用 Excel 打開，寫感謝卡或訂彌月禮時直接照著名單走。認領錯了也可以在這裡幫對方取消。

**設定**
後台密碼、已刪除項目的還原、以及重設整份清單。

右邊（或手機上按右下角「預覽前台」）是即時預覽，存檔後會自動重新整理，可以切「手機 / 電腦」兩種寬度。

---

## C. 接上 Google 試算表，讓大家共用（約 10 分鐘）

沒有這一步，每個人看到的認領狀態都是各自獨立的，就失去意義了。

**1.** 到 [sheets.new](https://sheets.new) 開一個新的空白試算表，命名為「Rae 育兒清單」。

**2.** 上方選單 **擴充功能 → Apps Script**。

**3.** 把編輯器裡原本的 `function myFunction() {}` 全部刪掉，貼上 `apps-script.gs` 的**全部內容**。

**4.** 找到最上面這一行，把密碼改成你自己的（等一下後台要用同一組）：

```js
var ADMIN_KEY = 'rae2026';
```

按存檔（磁片圖示）。

**5.** 右上角按 **部署 → 新增部署作業**。

**6.** 左邊齒輪 **選取類型 → 網頁應用程式**，然後設定：

- **執行身分**：`我`
- **具有存取權的使用者**：`所有人` ← 一定要選這個，親友才不用登入 Google

**7.** 按 **部署**。第一次會要求授權：按「授權存取權」→ 選你的 Google 帳號 → 出現「Google 尚未驗證這個應用程式」時，點左下角 **進階 → 前往「專案名稱」(不安全)** → **允許**。

> 這個警告是因為程式是你自己的、沒有經過 Google 商店審核，不是真的有問題。

**8.** 複製畫面上那串 **網頁應用程式網址**，長得像：

```
https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxxxxxxxxxxxxxx/exec
```

**9.** 用記事本打開 `registry-core.js`，找到最上面那行：

```js
const API_URL = "";
```

把網址貼進去：

```js
const API_URL = "https://script.google.com/macros/s/AKfycb.../exec";
```

存檔。

**10.** 重新整理 `admin.html`，左上角會從「本機模式」變成「已連線」。到 **設定** 分頁，把第 4 步設的密碼填進「後台密碼」按儲存。

> 密碼只存在你這台裝置的瀏覽器裡，不會出現在任何一個檔案中。換電腦編輯的話要再填一次。

---

## D. 放到網路上（約 5 分鐘）

親友要能用手機點開，檔案必須放在網路上。最省事的做法：

**Netlify Drop（不用寫任何指令）**

1. 到 <https://app.netlify.com/drop>
2. 把整個 `rae-registry` **資料夾**拖進去（不是單一檔案）
3. 馬上得到一個網址，例如 `https://xxxx-yyyy.netlify.app`
4. 註冊免費帳號（用 Google 登入即可）把它保存下來，否則網站過一陣子會消失
5. 在 **Site settings → Change site name** 可以改成好記的名字

之後就都在線上的 `admin.html` 編輯，不必再重傳檔案。只有改到 `registry-core.js` 或版面時才需要重新拖一次。

**你的兩個網址**

- 給親友：`https://你的網址.netlify.app`
- 你自己編輯：`https://你的網址.netlify.app/admin.html`

後台網址不要分享出去。就算有人猜到，沒有後台密碼也改不了東西（前提是你做完了 C 的第 4 和第 10 步）。

---

## E. 常見狀況

**Q：後台顯示「後台密碼不正確」**
`apps-script.gs` 裡的 `ADMIN_KEY` 和後台「設定」分頁填的要一模一樣。改過 `.gs` 之後記得重新部署（見下一題）。

**Q：改了 apps-script.gs 但沒有生效**
要重新 **部署 → 管理部署作業 → 鉛筆圖示 → 版本選「新版本」→ 部署**。網址不會變。

**Q：親友說按了沒反應**
確認 Apps Script 部署設定是「具有存取權的使用者：所有人」。認領和新增項目都不需要密碼，只有編輯清單需要。

**Q：兩個人同時認領同一項會怎樣？**
後端有加鎖，晚一步的那位會看到「這一項剛剛被別人認領了」，不會兩個人都以為自己搶到。

**Q：可以隱藏認領者的名字，保留驚喜嗎？**
Babylist 的預設是爸媽看不到誰買了什麼。這份清單反過來——大家都看得到名字，因為台灣送禮的重點通常在「心意被記得」。想改成不顯示的話跟我說。

**Q：資料會不會不見？**
資料在你自己的 Google 試算表裡（`_data` 分頁是原始資料，`認領紀錄` 分頁是整理好的表格），Google 帳號還在就在。想備份就「檔案 → 下載 → Excel」。

**Q：想加新項目，但已經上線了**
直接在後台加就好，不用去改 `registry-core.js`。那支檔案裡的清單只有「第一次建立時」會用到。
